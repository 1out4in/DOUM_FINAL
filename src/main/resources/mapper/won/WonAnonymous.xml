<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.doum.mapper.won.WonAnonymousMapper">

    <!-- 모든 익명 게시글 조회 -->
    <select id="selectAllAnonymousPosts">
        SELECT title, content, created_date AS createdDate, views
        FROM tbl_anonymous
    </select>

</mapper>